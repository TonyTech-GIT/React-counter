import Parent from "./components/Parent";

function App() {
  return (
    <div className="container">
      {/* One component on app for simplicity sake... */}
      <Parent />
    </div>
  );
}

export default App;
